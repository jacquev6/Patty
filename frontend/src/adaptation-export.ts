import { createApp } from 'vue'
import 'modern-normalize/modern-normalize.css'

import AdaptationExportView from './AdaptationExportView.vue'

const app = createApp(AdaptationExportView)

app.mount('#app')
